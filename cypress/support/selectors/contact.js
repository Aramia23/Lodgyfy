const nameInput= "[name='name']";
const phoneInput = "[data-testid='phone-input']";
const emailInput= "[name='email']";
const guestsInput= "[name='guests']";
const arrivalInput= "[aria-label='Arrival']";
const commentTextArea="[placeholder='Comment']";
const submitButton = "button[type='submit']";
const errorAlert = ".ui.input.error";
const startDate = "[aria-label='Thursday, April 14, 2022']"
const endDate = "[aria-label='Tuesday, June 14, 2022']"
const datePickerNextMonth = "[aria-label='Move forward to switch to the next month.']"

module.exports = {
    nameInput,
    phoneInput,
    emailInput,
    arrivalInput,
    commentTextArea,
    submitButton,
    guestsInput,
    errorAlert,
    startDate,
    endDate,
    datePickerNextMonth
}